# Verified working together in app/.venv (Python 3.12.10, Windows, CPU-only)
fastapi==0.115.6
starlette==0.41.3
uvicorn==0.34.0
jinja2==3.1.6
pydantic==2.10.5

# Real speech-to-text (CTranslate2 backend, CPU int8)
faster-whisper==1.2.1
ctranslate2==4.7.2

# Real DSP feature extraction for signal-derived emotion analysis
librosa==0.11.0
numpy==2.4.6

# WebSocket support (uvicorn[standard] wsproto/websockets, used by test client too)
websockets==16.0
